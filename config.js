// ---------------------------------------------------------------------------
// Paste your Google Apps Script Web App URL between the quotes below.
// (See README.md, step "Set up the Google Sheet backend".)
// Until you do, answers are still saved locally in your browser and can be
// exported as CSV with the "Export my data" link.
// ---------------------------------------------------------------------------
window.APP_CONFIG = {
  endpoint: ""   // e.g. "https://script.google.com/macros/s/AKfy.../exec"
};
